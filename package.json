{
  "name": "plier-manifesto-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:pdf": "node scripts/generate-manifesto-pdf.cjs"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "pdfkit": "^0.18.0"
  }
}
